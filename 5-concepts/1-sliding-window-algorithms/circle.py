#!/usr/bin/python3
from math import pi


def circle_area(self, radius):
    """Returns the area of a circle

    Args:
        radius(int): input radius
        of the circle

    """
    return pi * (radius ** 2)
